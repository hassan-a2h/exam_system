<nav class="navbar navbar-dark bg-dark">
  <%= link_to 'Home', root_path, class: 'btn btn-outline-primary' %>
  <% if user_signed_in? %>
    <% if current_user.Admin? %>
      <%= link_to 'Manage Users', users_path, class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Manage Subjects', admin_subjects_path(current_user), class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Manage Exams', exams_path, class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Manage Schedules', schedules_path, class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Results', results_path, class: 'btn btn-primary mx-auto' %>

    <% elsif current_user.Teacher? %>
      <%= link_to 'Manage Exams', exams_path, class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Manage Schedules', schedules_path, class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Results', results_path, class: 'btn btn-primary mx-auto mx-auto' %>

    <% else %>
      <%= link_to 'Active exams', schedules_path, class: 'btn btn-primary mx-auto' %>
      <%= link_to 'Results', results_path, class: 'btn btn-primary mx-auto' %>
    <% end %>

    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-danger ml-auto' %>
  <% else %>
    <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-primary ml-auto' %>
  <% end %>
</nav>
